run ```pip install requirement.txt```
and then run the ipynb on the two dataset. The Fetal heart rate data is included in the folder while the mnist dataset can be easily downloaded through importingkeras.dataset. Two notebook files is also included in the directory. run them to recreate the code

